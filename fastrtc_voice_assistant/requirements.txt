# Core FastRTC dependencies
fastrtc[vad,stt,tts,stopword]
requests
soundfile
chromadb>=0.4.22
sentence-transformers>=2.2.2
rank_bm25>=0.2.2

# Mem0 dependencies (new)
mem0ai

# LLM dependencies for A-MEM
litellm
nltk

# HTTP and async
aiohttp

transformers>=4.36.2

# FastRTC Whisper
fastrtc-whisper-cpp

# Faster-whisper dependencies (CTranslate2 backend)
faster-whisper>=1.1           # CTranslate2 backend, streaming API
ctranslate2>=4.2.0            # INT8/FP16 kernels, FlashAttn optional
huggingface_hub>=0.25         # HuggingFace model hub
hf_transfer                   # Rust-based fast downloader (optional)

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
ollama
# Core FastRTC dependencies
fastrtc[vad,stt,tts,stopword]
requests
soundfile
chromadb>=0.4.22
sentence-transformers>=2.2.2
rank_bm25>=0.2.2
librosa==0.11.0
soxr==0.5.0.post1
fastapi
uvicorn[standard]


# LLM dependencies for A-MEM
litellm
nltk
torch>=2.1.1

# HTTP and async
aiohttp
transformers>=4.36.2
numpy==1.26.4

# Faster-whisper dependencies (CTranslate2 backend)
faster-whisper>=1.1           # CTranslate2 backend, streaming API
ctranslate2>=4.2.0            # INT8/FP16 kernels, FlashAttn optional
huggingface_hub[hf_transfer]>=0.25  # Rust downloader for model layer

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
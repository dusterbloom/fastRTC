## Run first pip install -r requirements.txt
## Then run pip install --no-deps fastrtc==0.0.28

numpy==1.26.4  # numpy<2.0 for compatibility with transformers and other libraries
torch==2.1.1
# Other top-level packages your app uses directly
fastapi
uvicorn[standard]
librosa
scipy
gradio

requests
soundfile
chromadb
rank_bm25
librosa
soxr

# LLM dependencies for A-MEM
nltk

# HTTP and async
aiohttp
aioice>=0.10.1
aiortc
transformers

# Faster-whisper dependencies (CTranslate2 backend)
faster-whisper           # CTranslate2 backend, streaming API
ctranslate2==4.4.0            # INT8/FP16 kernels, FlashAttn optional
huggingface_hub[hf_transfer]  # Rust downloader for model layer
sentencepiece
pyaudio

# Development and testing
pytest
pytest-asyncio


# Ollama dependencies
ollama

# Redis dependencies
redis

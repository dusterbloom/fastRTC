fastrtc[vad,stt,tts,stopword]
requests
soundfile
chromadb>=0.4.0
sentence-transformers>=2.2.0

# Mem0 dependencies (new)
mem0ai

# Optional: For local embeddings (recommended)
ollama (install separately)

aiohttp
asyncio

# Optional: For better performance
# qdrant-client (if using Qdrant vector store)
# neo4j (if using graph memory)